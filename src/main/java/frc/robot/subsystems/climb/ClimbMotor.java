package frc.robot.subsystems.climb;

import frc.robot.subsystems.elevator.ElevatorConstants;


public class ClimbMotor {
    public class MotorUtil {

    public static ClimbSpark createSparkMAX(int climbMotorId, ElevatorConstants motorType, int currentLimit, boolean inverted,
            boolean brakeMode, double rampRate) {
        ClimbSpark sparkMax = new ClimbSpark();
        return sparkMax;
    }
}
}
